# D4Hub v1.0 Release Readiness Control

Last live audit: 2026-07-23 (Asia/Shanghai)

Manager mode: manager-only

Release intent: Owner-verifiable, unsigned, local-only v1.0 candidate. This is not authorization to sign, upload, host, update, distribute, or publish.

Active slice: **none**. V10-E03-S1 is independently verified within its local diagnostic-tool boundary. The next valid action requires Owner-run Diablo IV client-surface captures and labels; detector tuning, target replay, candidate freeze, packaging, commit, push, and publication remain blocked.

Active worker limit: **one**. Every worker result is provisional until the manager inspects its complete diff/evidence and independently reruns the named commands.

Candidate SHA: **none**. Git HEAD `8e944d23bead51ecc1e2a386b8e088f0ceb0e722` contains only the public-boundary initialization; the product tree is not frozen or committed.

## Goal And Safety Boundary

Converge the existing working tree into a traceable, reproducible, unsigned local v1.0 candidate that provides screen-only HUD recognition, per-BD/per-resolution calibration, local persistence, the governed D2Core bundled-library/cache/import boundary, hotkeys and click-through behavior, and redacted local diagnostic export.

The candidate must not read or write game memory, inject code, automate input, provide macros, evade detection, copy reference-project binaries/assets, or add a new third-party network integration. `.pkr/`, `.tools/`, `.artifacts/`, `publish/`, and the downloaded technical reference are private/local boundaries and must not enter the public source tree.

## Live Truth

- Git status: `.gitignore` is modified; `.github/`, `D4Hub.sln`, `LICENSE`, `README.md`, `docs/`, `library/`, `scripts/`, `src/`, `tests/`, and `tools/` are untracked user work. Preserve all unknown changes.
- Git history: one commit, `8e944d2 chore: initialize public project boundary`; only `.gitignore` is currently tracked.
- PKR SQLite authority: `.pkr/runtime.sqlite`, project `project_d4hub_001`, phase `active`, sequence `60`, current digest `ebb8e01549f70077f5e8352fb24dfe5c415dfb2e54b303fc602132195d9417ee`. Before setup the live sequence was `48`; Goal creation was sequence `49`, V10-E01 Task creation was sequence `50`, the independently checked blocker summary was recorded as project Memory at sequence `51`, V10-D01-S1 Task creation was sequence `52`, its manager verification summary was recorded as project Memory at sequence `53`, the corpus-intake manager review was recorded as Memory `memory_b1e05cd30e79d2fbaf9ae6bef5df2dcc` at sequence `54`, V10-E02-S1 Task `task_270af1018119e61c03fefe5ca07cef68` was created at sequence `55`, its independent manager review was recorded as Memory `memory_d7d5b9c3d14b7bb916e8b2b942d838f8` at sequence `56`, V10-E02-S2 Task `task_3a7ebeb946cfdce1df842b83b5ef123b` was created at sequence `57`, its independent manager review was recorded as Memory `memory_0136ea930a50933cbddf780f9823b119` at sequence `58`, V10-E03-S1 Task `task_d4f87aa873cc20099f22587e29bf6ee9` was created at sequence `59`, and its independent manager review was recorded as Memory `memory_3864963c0cf7ef22f4b7630ee8c70f61` at sequence `60`.
- New PKR records: Goal `goal_9629c8dedf1dbb4812929f36af42c778` (`proposed`) and Task `task_dd62a03b6e0028db49e2dda9417d3581` (`backlog`). They mirror this board; no claim, Assignment, Lease, Verification, or Acceptance is fabricated.
- V10-E01 manager-review Memories: `memory_1ce92fe3851fb9ac73af8975d52e05bf` (pre-intake blocker review) and `memory_b1e05cd30e79d2fbaf9ae6bef5df2dcc` (current 17-file corpus intake review), both derived from Task revision `1`. The current Memory records the independently reproduced 17/17 inventory, exact target counts `0/12`, uncertain HUD classification, unproven BD/role/provenance/privacy clearance, and canonical `44/44`; the Task remains `backlog` with no Verification or Acceptance.
- PKR now has six Goals and thirteen Tasks; the pre-setup baseline had five Goals and eight Tasks. V10-D01-S1 is mirrored as Task `task_0501fbe15193abc1b6ca3a0105929934` (`backlog`); repo-board dispatch does not fabricate a PKR Assignment. Its manager verification summary is Memory `memory_8d5ffb08b421b74f402c9c31371a69da`. Historical Task `task_dc22a4ce0ee362474ed9ab8de47ce3da` and Assignment `assignment_103c0d3ae3bd88236965b1f730aebaa8` remain `inProgress`/`running` with last verified activity on 2026-07-20. They are stale but are not closed or reused without evidence.
- v0.2 mirror Tasks V02-S1, V02-S2, and V02-S3 remain Runtime `backlog`. H01 Task `task_800a01630b0e2cdbbf051d269ec1049e` also remains `backlog`.
- V02-S1 and V02-S2 are implemented, locally tested, and independently verified only within their diagnostic/harness boundaries. H01 is independently verified only as a local manifest/hash contract. None of these records proves a v1.0 candidate.
- Current source implements the principal screen-only HUD, recognition, layout templates, persistence, D2Core library/cache/import, global hotkey, click-through, preview, and local update-manifest contract behaviors.
- Canonical verifier: `powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1`. Its deterministic checks do not prove real-game, UI/device, soak, clean-machine, reproducible-build, or Owner acceptance gates.
- Local ignored packages exist under `publish/`, but none is a v1.0 package and none has established exact-source provenance.
- `.artifacts/v0.2-real-frames/` currently contains replay harness manifests/reports and one copied 1404x841 HUD-composited smoke frame. It contains no raw 1920x1080/1280x960 target matrix.
- Owner approved first-capture fingerprints, per-BD/per-resolution calibration templates, bundled D2Core public library plus local cache, the provided HUD symbols, and an unsigned local preview for Owner validation.
- Owner explicitly deferred online release, signing, hosting, installer/updater, upload, real-player distribution, telemetry, support/EOL policy, and new third-party integration.

## v1.0 State Classification

| State | Current classification | Evidence boundary |
| --- | --- | --- |
| implemented | **partial, working tree only** | Principal product behaviors exist; diagnostic export and some release-readiness behavior are still unproven or absent. |
| tested | **partial** | Synthetic/fixture and local deterministic checks only; prior results must be rerun against the current tree. |
| packaged | **no v1.0 package** | Ignored historical local folders are not exact-SHA v1.0 artifacts. |
| independently verified | **no at candidate level** | Prior slice verification cannot be promoted to candidate verification. |
| owner accepted | **no** | Owner decisions approve direction and local preview only, not a named SHA/package hash. |
| committed | **no product candidate** | HEAD does not contain product source. |
| pushed | **no evidence and no authorization** | No authorized remote evidence for a candidate SHA. |
| published | **no; blocked/deferred** | No signing, upload, Release, production channel, or player distribution is authorized. |

These states are independent and must never be inferred from one another.

## Prerequisite Chain

v1.0 is an aggregation gate, not a renaming of existing local work:

1. **v0.2 recognition evidence:** target-resolution, multi-BD, unknown-BD, and panel-absent replay evidence. V02-S3 remains blocked and is the first v1.0 release blocker.
2. **v0.3 HUD/device acceptance:** per-BD/per-resolution calibration persistence, restart recovery, non-occlusion, window/foreground behavior, hotkey/click-through, and physical-device Owner review. Blocked by v0.2.
3. **v0.4 data reliability:** local persistence, D2Core bundled/cache/import boundary, corruption recovery, backup/restore, failure preservation, and offline operation. Blocked by v0.3.
4. **v0.5 candidate engineering:** soak/resources, accessibility, redacted diagnostics, Release build, clean-machine instructions, repeatable builds, manifest/artifact hashes, exact candidate SHA, and independent verification. Blocked by v0.4.
5. **v1.0 governance closure:** Owner acceptance of the exact SHA and package hashes, PKR Task/Verification evidence, and an authorized Git commit. Push and publication remain separate.

H01 supplies only a future local artifact-integrity primitive. It is not an updater, packaging gate, candidate freeze, Owner acceptance, or release authorization.

## Gate Map And Task Cards

### V10-E01 Real-Frame Evidence And Replay - ACTIVE CORPUS INTAKE / TARGET GATE BLOCKED

- Outcome: determine recognition reliability using Owner-provided raw client-surface PNGs, not fixtures or composited previews.
- Required input: twelve raw PNGs under `.artifacts/v0.2-real-frames/target/`:
  - 1920x1080: known BD-A panel open twice, known BD-B panel open twice, one unknown/unregistered BD panel open, one panel-absent frame.
  - 1280x960: the same six cases.
- Capture requirements: exact client dimensions; HUD hidden; no crop, resize, or re-encoding; stable UI scale within each pair; visible skill bar; positive cases mapped to BD labels; account/chat information hidden before capture.
- Worker file fence: read-only access to existing source/tests/scripts and `.artifacts/v0.2-real-frames/**`; may write only `docs/manager/evidence/v1.0/V10-E01-provisional-2026-07-23.md`. No product, test, script, library, PKR, publish, tooling, workflow, or reference-project changes.
- Verification commands:
  - `powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1`
  - `powershell -ExecutionPolicy Bypass -File .\scripts\verify-vision-replay.ps1 -Manifest <target-manifest> -Report <local-report>` when the target corpus and manifest exist.
  - image dimension/hash inventory of every target input; replay report inspection; `git diff --check`.
- Failure: missing or uncertain provenance; wrong dimensions; only one BD; repeated positives are not stable; distinct BDs are not distinguishable; unknown BD or panel-absent frame is accepted; report labels fixture/smoke data as target evidence; any forbidden product change.
- Evidence paths: raw/local inputs and reports remain ignored under `.artifacts/v0.2-real-frames/target/`; provisional tracked worker report under `docs/manager/evidence/v1.0/`; independently reviewed final report will use `V10-E01-verification-<date>.md`.
- Current intake: `.artifacts/v0.2-real-frames/target/` contains 17 user PNGs and no `manifest.json`. The manager independently reproduced all 17 byte counts, PNG-header dimensions, and SHA-256 values; exact `1920x1080` count is `0` and exact `1280x960` count is `0`. Images that differ only by borders or window chrome are not exact target cases and must not be cropped, resized, or relabeled as such.
- Current blocker: the required exact twelve-case target matrix remains unproven. The provisional report at `docs/manager/evidence/v1.0/V10-E01-corpus-intake-provisional-2026-07-23.md` records all 17 as exploratory/smoke only: full client surface, character panel open, and skill bar visible; D4Hub HUD `uncertain`; BD identity, repeated pairs, unknown/panel-absent role, provenance, and privacy clearance unproven. No target manifest or replay was created. Owner must provide exact `1920x1080` and `1280x960` cases, labels, provenance, and privacy-cleared captures before a manifest can be considered.
- Candidate SHA: none.
- Owner gate: Owner supplies or explicitly approves provenance for the exact twelve-image corpus and later accepts or rejects the exact frozen candidate.
- Worker delivery acceptance: **accepted only as accurate factual blocker evidence**. The provisional report correctly establishes target corpus `0/12`, missing-manifest exit `2`, and smoke/non-target classification. This does not implement, test, independently verify, or accept the V10-E01 target gate.

#### V10-E01 Corpus Intake Audit - ACTIVE

- Outcome: inventory and visually classify all 17 PNGs without modifying them, and state exactly which are useful only as exploratory/smoke evidence versus which target requirements remain missing.
- Worker file fence: read-only access to `.artifacts/v0.2-real-frames/target/*.png`, the existing replay contract/scripts, and manager evidence/control documents; the only permitted write is `docs/manager/evidence/v1.0/V10-E01-corpus-intake-provisional-2026-07-23.md`.
- Required report fields per image: filename, byte length, exact dimensions, SHA-256, whether it visibly appears to be a full game/client surface, whether D4Hub HUD is visibly absent/present/uncertain, whether the character panel and skill bar are visible, privacy exposure, and possible repeated-pair/known-BD/unknown-BD/panel-absent role expressed only as `candidate` or `unproven`.
- Prohibited: modifying, cropping, resizing, re-encoding, renaming, deleting, or generating derivatives of user images; guessing BD identity or provenance; creating `manifest.json`; running a target replay without exact dimensions and Owner labels; changing product/tests/scripts/library/PKR/tooling/publish/reference files.
- Acceptance: all 17 files accounted for; hashes/dimensions independently reproducible; classifications are visibly supportable and use `uncertain` where necessary; no exact target, provenance, BD, or Owner claim is inferred; remaining Owner inputs are enumerated precisely.
- Failure: any image mutation; any near-size image promoted to exact target; privacy content copied into tracked evidence; unproven labels stated as fact; target manifest or acceptance created without the required evidence.
- Candidate SHA: none. Packaging, signing, hosting, upload, production, commit, push, and publication remain prohibited.

### V10-E02-S1 Client-Surface Evidence Contract - INDEPENDENTLY VERIFIED / NOT OWNER ACCEPTED

- PKR Task: `task_270af1018119e61c03fefe5ca07cef68`, revision `1`, phase `backlog`. Repo-board worker dispatch does not fabricate an Assignment, Lease, Verification, or Acceptance.
- User scenario: a player may run Diablo IV windowed, borderless, or fullscreen on a DPI-scaled desktop. When D4Hub says a resolution is supported or selects a per-BD/per-resolution layout template, the dimensions must mean the captured game **client area in device pixels**, not monitor resolution, WPF device-independent units, or a PNG that includes title bars and borders.
- Existing authority: Owner decision D4HUD-D002 approves a template for each BD and each game-client resolution. `GameWindowLocator` uses `GetClientRect` plus `ClientToScreen`; `ScreenFrameService` allocates and captures that width/height; `HudViewModel` selects exact `ClientWidth`/`ClientHeight` templates; the app is PerMonitorV2 DPI aware. V10-E02-S1 codifies these existing facts and does not authorize a new capture mode.
- Input boundary:
  - Runtime recognition input is the Win32 client rectangle returned by the located game window. Non-client title bars, resize borders, and window shadows are outside the coordinate space.
  - Letterbox/pillarbox pixels inside the client rectangle remain part of the raw frame. The detector may use normalized active-viewport bounds, but the frame must not be cropped, resized, or re-encoded to manufacture a supported resolution.
  - Offline replay `expectedWidth`/`expectedHeight` refer to the raw PNG dimensions in the declared capture space. A target case must explicitly declare `captureSpace: client-device-pixels`; full-window, unknown, or missing capture space is non-target.
  - Normalized detector and fingerprint coordinates are relative to the complete client-surface frame. HUD layout template identity remains exact BD plus client width/height; no near-size fallback is introduced.
- DPI/scaling and borders: the contract is physical/device pixels under the existing PerMonitorV2 process declaration. Desktop scaling must not convert target dimensions to WPF DIPs. Window chrome is excluded only by capture origin/extent, never by post-processing an Owner PNG.
- Privacy and provenance: declaring `captureSpace` is not provenance. Target evidence still needs Owner-approved source, exact SHA-256, role/BD labels, HUD-hidden confirmation, and privacy clearance. Reports may record privacy categories but must not transcribe visible names, chat, account data, or currency values.
- Positive examples: a raw client-only PNG whose pixels are exactly `1920x1080` or `1280x960`; a client frame with internal black bars whose outer client dimensions remain exact; an exact per-BD/per-client-size template match.
- Negative examples: any of the current 17 exploratory full-window PNGs; `1922x1112`, `1282x992`, or other near-size images; a screenshot whose chrome was later cropped; missing/unknown/full-window capture space in a target manifest; automatic reuse of a template from a different client size.
- Failure behavior: reject malformed or non-client target declarations before launching VisionProbe; use exit `2` for manifest/contract errors; do not emit a target replay report from validation-only execution; do not mutate input images or silently normalize dimensions. Smoke/exploratory inputs may retain an explicit non-client/unknown declaration but can never satisfy target evidence.
- Acceptance matrix:
  - target manifest with `client-device-pixels` and exact required resolution representatives passes schema-only validation but is not replay evidence;
  - target manifest with missing, unknown, or full-window capture space fails before image/probe access;
  - target manifest with only near-size dimensions fails the existing exact-resolution requirement;
  - smoke manifest can explicitly describe non-client/unknown material and remains non-gating;
  - validation-only success is labeled schema-only and cannot create a replay report;
  - canonical verifier runs deterministic contract checks and remains green.
- Worker file fence:
  - `scripts/verify-vision-replay.ps1`
  - `scripts/verify.ps1`
  - `tests/vision-replay/README.md`
  - `tests/vision-replay/manifest.sample.json`
  - one new deterministic contract test under `tests/vision-replay/`
  - `docs/manager/evidence/v1.0/V10-E02-S1-provisional-2026-07-23.md`
- Non-goals and prohibited actions: no detector/fingerprint threshold changes; no product source or WPF changes; no new supported resolution; no reinterpretation of the V10-E01 twelve-case matrix; no target manifest or replay using the 17 images; no image modification/derivative; no memory access, injection, automated input, macro, evasion, reference asset copying, network/provider change, packaging, commit, push, signing, upload, or publication.
- Owner decision point: none for this slice because D4HUD-D002 already selects game-client resolution. Any future proposal to support full-window uploads, infer/crop chrome automatically, use logical pixels, or accept aspect-ratio/near-size fallback requires a new explicit Owner decision.
- Requirements classification: `requirements defined=yes`; `implemented=yes`; `tested=yes`; `independently verified=yes within the replay-contract boundary`; `owner accepted=existing D4HUD-D002 direction only, not this delivery`; `committed=no`; `pushed=no`; `published=no`; candidate SHA `none`. Manager Memory: `memory_d7d5b9c3d14b7bb916e8b2b942d838f8`, project sequence `56`.

### V10-E02-S2 Mixed-Resolution Exploratory Replay - INDEPENDENTLY VERIFIED FAILED SMOKE EVIDENCE

- Outcome: run all 17 existing native PNGs through the current offline replay harness and measure panel-detection behavior across their actual dimensions without changing any input, threshold, detector, fingerprint, or target requirement.
- PKR Task: `task_3a7ebeb946cfdce1df842b83b5ef123b`, revision `1`, phase `backlog`, created at project sequence `57` under Goal `goal_9629c8dedf1dbb4812929f36af42c778`. Repo-board dispatch does not fabricate an Assignment, Lease, Verification, or Acceptance.
- Evidence class and coordinate boundary: every case is `smoke`, declares `captureSpace: unknown`, uses the exact PNG-header width and height, and is permanently non-gating. The manifest must not state or imply client-only provenance, exact target resolution, BD identity, repeated-pair binding, unknown-BD role, panel-absent role, HUD-hidden status, privacy clearance, or Owner acceptance.
- Expected behavior: because the intake review visibly found the character panel open in all 17 images, each case may use the unchanged default `panelThreshold: 0.55` and `expectedDecision: accepted`. Any rejected case is a useful diagnostic failure and must remain failed; the worker must not lower a threshold, weaken an assertion, delete a case, crop/resize/re-encode an image, or change product/test/script code.
- Worker file fence:
  - read-only: the 17 original `.artifacts/v0.2-real-frames/target/*.png`, `scripts/verify-vision-replay.ps1`, `tests/D4Hub.VisionProbe/**`, the E02-S1 contract/report, and the corpus-intake report;
  - local ignored writes only: `.artifacts/v0.2-real-frames/target/exploratory-smoke-manifest.json` and `.artifacts/v0.2-real-frames/target/exploratory-smoke-report.json`;
  - tracked write only: `docs/manager/evidence/v1.0/V10-E02-S2-provisional-2026-07-23.md`.
- Verification commands:
  - `powershell -ExecutionPolicy Bypass -File .\scripts\verify-vision-replay.ps1 -Manifest .\.artifacts\v0.2-real-frames\target\exploratory-smoke-manifest.json -Output .\.artifacts\v0.2-real-frames\target\exploratory-smoke-report.json`
  - `powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1`
  - independent inventory comparing all manifest paths, dimensions, byte counts, and SHA-256 values with the 17 originals; `git diff --check`.
- Acceptance: all 17 originals appear exactly once; no input hash changes; the manifest/report remain ignored and label evidence as smoke/unknown; replay output records every actual decision and exits `0` only if all declared expectations pass; the tracked report contains only aggregate or opaque file identifiers and does not transcribe visible personal/account/game values.
- Failure: missing/duplicated input; altered image hash; fabricated coordinate provenance or labels; a threshold other than `0.55`; suppression of a rejected case; any source/test/script/PKR/package/publish/reference change by the worker; any claim that this replay satisfies V10-E01 or accepts a candidate.
- Manager result: the replay reproducibly exits `5` with `6/17` accepted and `11/17` rejected at the unchanged `0.55` threshold. This is `implemented=local exploratory evidence generated`, `tested=yes with assertion failure preserved`, and `independently verified=yes for the smoke result`; it is not target evidence, a proven client-surface defect, BD stability/distinction, Owner acceptance, or candidate evidence. Manager Memory: `memory_0136ea930a50933cbddf780f9823b119`, project sequence `58`.
- Remaining classification: `owner accepted=no`; `target evidence=no`; `candidate SHA=none`; `committed=no`; `pushed=no`; `published=no`.

### V10-E03-S1 Client-Surface Capture Receipt - INDEPENDENTLY VERIFIED / NOT OWNER ACCEPTED

- Outcome: make the existing explicit `D4Hub.VisionProbe --capture-title` and `--capture-screen-title` commands emit a machine-readable local receipt that binds the saved PNG to `captureSpace: client-device-pixels`, exact width/height, byte length, SHA-256, output path, and capture method.
- PKR Task: `task_d4f87aa873cc20099f22587e29bf6ee9`, revision `1`, phase `backlog`, created at project sequence `59` under Goal `goal_9629c8dedf1dbb4812929f36af42c778`. Repo-board dispatch does not fabricate an Assignment, Lease, Verification, or Acceptance.
- Privacy boundary: the JSON receipt must not emit the window title, handle, desktop position, account/player text, pixels, or any external destination. The screenshot stays at the explicit local output path; no upload, clipboard, telemetry, network, or automatic input is added.
- Coordinate boundary: both current capture paths use the `GameClientWindow` client rectangle. The receipt must distinguish the current methods (PrintWindow client-only versus screen copy of the client rectangle) while declaring the same `client-device-pixels` coordinate space. It must not claim Diablo provenance merely because an arbitrary title matched.
- Overwrite and failure behavior: preserve existing CLI command names, fail explicitly when the named visible non-minimized window is missing or capture/save fails, and do not emit a success receipt unless the PNG exists and its post-write bytes/hash/dimensions match the receipt. Existing overwrite behavior is not widened in this slice; any future overwrite-policy change requires a separate card.
- Worker file fence:
  - `tests/D4Hub.VisionProbe/Program.cs`
  - one new deterministic receipt verification script under `tests/vision-replay/`
  - `scripts/verify.ps1`
  - `tests/vision-replay/README.md`
  - `docs/manager/evidence/v1.0/V10-E03-S1-provisional-2026-07-23.md`
- Deterministic verification: use the existing local `D4Hub.GameWindowFixture` and a repository image only as a temporary screenshot host; start and stop it within the test, capture to a unique system temporary directory, validate receipt schema, PNG header dimensions, bytes, SHA-256, capture method/space, absence of privacy fields, and cleanup. Cover missing-window failure with no output/receipt. The test must not depend on Diablo IV, `.artifacts`, network, external tools, or permanent files.
- Canonical verification: the new deterministic script must be invoked by `scripts/verify.ps1`; also run Release build, existing `44/44` acceptance checks, E02 coordinate contract, safety scan, `git diff --check`, and manager line-by-line diff review.
- Failure: incorrect/non-client dimensions; receipt emitted before or without a matching PNG; leaked title/handle/position; test leaves a process or file behind; dependence on ignored user images; any detector/threshold/fingerprint/product UI/PKR/package/release change; any claim that a receipt establishes Owner provenance, privacy clearance, target replay success, or candidate acceptance.
- Manager result: `requirements defined=yes`; `implemented=yes`; `tested=yes`; `independently verified=yes within the local diagnostic-tool boundary`; `owner accepted=no`; `target evidence=no`; `candidate SHA=none`; `committed=no`; `pushed=no`; `published=no`. Manager Memory: `memory_3864963c0cf7ef22f4b7630ee8c70f61`, project sequence `60`.

### V10-U01 HUD Calibration And Physical Device - BACKLOG

- Outcome: prove 1920x1080/1280x960 layout, per-BD/per-resolution save/load/reset, restart recovery, non-occlusion, foreground/window changes, hotkey, click-through, and visible symbols on a physical device.
- File fence: only the minimum `src/D4Hub.App/**` and calibration-focused tests named in a later active card; local captures remain ignored.
- Evidence: `docs/manager/evidence/v1.0/V10-U01-*.md` plus ignored local captures.
- Failure: inaccessible controls, unreadable symbols, obstructed equipment slots, layout cross-contamination, broken hotkey/click-through, or fixture/automation substituted for physical/Owner acceptance.
- Owner gate: physical-device visual and interaction acceptance. Blocked by V10-E01.

### V10-D01 Persistence, D2Core Boundary, Recovery, Offline - BACKLOG

- Outcome: prove local state round trips and backup/restore; corrupt or partial state fails safely; existing data survives failed imports/writes; bundled library and explicit cache/import behavior work offline without automatic upload or new provider access.
- File fence: the minimum `src/D4Hub.Core/**`, persistence/library-focused app services, `library/**`, and matching tests named in a later active card.
- Evidence: `docs/manager/evidence/v1.0/V10-D01-*.md`; private fixtures remain ignored.
- Verification: canonical verifier; network-denial run; before/after byte and hash checks; corruption, interrupted-write, cache-hit/miss, restore, and offline replays.
- Failure: data loss, silent corruption acceptance, automatic sharing/upload, network dependency for previously cached/local use, or new third-party integration.
- Owner gate: representative recovery/offline matrix approval. Blocked by V10-U01.

### V10-D01-S1 Corrupt/Incomplete Import Preservation - INDEPENDENTLY VERIFIED / NOT OWNER ACCEPTED

- Outcome: preserve the startup recovery behavior for a damaged local state file, while making explicit user imports of missing, malformed, empty, or model-invalid JSON fail visibly without replacing or saving over the currently loaded valid document.
- Why now: this is the highest-value repository-contained data-loss risk that does not depend on the missing real screenshots. It advances only a deterministic v0.4/v1.0 prerequisite; it does not bypass the v0.2/v0.3 Owner gates or authorize later packaging.
- Worker file fence:
  - `src/D4Hub.Core/JsonStateStore.cs`
  - `src/D4Hub.App/ViewModels/MainViewModel.cs`
  - `src/D4Hub.App/ViewModels/HudViewModel.cs`
  - import/recovery-only additions in `tests/D4Hub.AcceptanceTests/Program.cs`
  - `docs/manager/evidence/v1.0/V10-D01-S1-provisional-2026-07-23.md`
- Acceptance:
  - startup `Load()` still returns a usable starter document for a missing/damaged local state file;
  - a strict import path rejects a missing file, malformed JSON, empty/null JSON, and structurally invalid documents with an explicit exception/error result;
  - both product import entry points use the strict path and replace/save the active document only after complete validation;
  - a failed import leaves the current in-memory document and persisted state bytes/hash unchanged;
  - a valid export/import round trip still succeeds;
  - no `.tmp` files remain after successful saves and no failed import creates package/network/external effects.
- Verification: focused acceptance executable; `dotnet build .\D4Hub.sln -c Release`; `powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1`; before/after byte and SHA-256 assertions; forbidden-API scan; `git diff --check`; manager inspection of every changed line.
- Failure: a corrupt/incompatible import silently becomes starter data; existing in-memory or persisted state changes after a failed import; startup recovery regresses; valid import/export regresses; exceptions escape a UI import entry point; any file outside the fence changes.
- Prohibited: screenshot/recognition changes, D2Core network behavior, migration beyond the current schema, automatic backup policy, UI redesign, telemetry, new dependency, packaging, signing, hosting, upload, publication, PKR mutation by the worker, `.artifacts`, `.tools`, `publish`, or technical-reference changes.
- Evidence: worker report at the exact path above; manager verification will be appended to this control document after independent reruns.
- Candidate SHA: none. Owner acceptance, commit, push, and publication remain absent.
- Manager result: **implemented, tested, and independently verified within the local import-preservation contract**. This does not verify WPF physical behavior, the full v0.4 recovery/offline matrix, a v1.0 candidate, or any Owner gate. The PKR Task remains `backlog` because no Assignment, Lease, Verification, or Acceptance was fabricated.

### V10-Q01 Soak, Accessibility, Diagnostics - BACKLOG

- Outcome: bounded CPU/memory/handle behavior during a controlled soak; keyboard/focus/contrast/scaling review; user-triggered local diagnostic export with redaction and no automatic transmission.
- File fence: later card must name exact diagnostic/app/test files; no telemetry SDK or network sender.
- Evidence: `docs/manager/evidence/v1.0/V10-Q01-*.md` and ignored soak/output artifacts.
- Failure: sustained resource growth beyond the recorded threshold, inaccessible critical workflow, sensitive paths/account/game imagery in export, or any automatic upload.
- Owner gate: approve measured thresholds, accessibility residuals, and diagnostic schema. Blocked by V10-D01.

### V10-F01 Candidate Freeze And Reproducible Local Package - BACKLOG

- Outcome: freeze source, run Release build, produce two clean local builds, compare normalized artifacts, create a v1.0 manifest with exact source SHA and SHA-256 hashes, and provide clean-machine/offline run instructions plus README/license notices.
- File fence: release scripts/docs/workflow and version metadata explicitly listed in a later active card; package outputs stay ignored. `.pkr`, `.tools`, `.artifacts`, `publish`, and technical reference must remain outside public source.
- Verification: canonical verifier; clean build A/B; artifact inventory/hash comparison; forbidden-boundary scan; license/README review; clean-machine run.
- Failure: dirty source freeze, package not attributable to exact SHA, non-repeatable unexplained bytes, missing notices/instructions, private boundary inclusion, or packaging before preceding gates pass.
- Evidence: `docs/manager/evidence/v1.0/V10-F01-*.md`, ignored local builds, and a tracked non-secret manifest/report.
- Candidate SHA: assigned only after the product source is committed at this gate.
- Owner gate: none for preparing the unsigned local candidate; commit still requires explicit authorization. Blocked by V10-Q01.

### V10-V01 Independent Verification, Owner Acceptance, PKR/Git Closure - BACKLOG

- Outcome: an independent verifier inspects the exact candidate diff and evidence, reruns every required gate, records PKR Task/Verification state, and presents the exact SHA plus package hashes to Owner. After explicit Owner acceptance and commit authorization, record the accepted Git commit without pushing.
- File fence: manager evidence/control docs and supported PKR CLI records; no source edits during verification.
- Failure: worker self-verification treated as independent; any rerun failure; candidate changes after hashing; Owner acceptance lacks exact SHA/hash; projection used instead of SQLite; commit made without authorization; push/publication implied.
- Evidence: `docs/manager/evidence/v1.0/V10-V01-*.md`, PKR SQLite record IDs, Git SHA, candidate manifest and hashes.
- Owner gate: explicit acceptance of exact SHA and every package hash, plus separate commit authorization. Acceptance does not authorize push or publication.

### V10-R01 Formal Release - BLOCKED/DEFERRED

Signing, certificate/key custody, hosting, upload, installer/updater, production channels, a GitHub or other Release, real-player distribution, telemetry/crash reporting, rollout, and support/EOL policy require new explicit Owner decisions and separate production evidence. No local test, H01 hash check, package, commit, tag, or v1.0 content plan authorizes them.

## Manager Dispatch And Review Rules

- Exactly one active slice and one worker.
- Repo-board dispatch is provisional and does not fabricate a PKR Assignment while the historical Assignment remains unresolved.
- Each worker receives an exact file fence and must report changed files, commands, outputs, residual risks, and blockers.
- The manager must inspect the complete result, rerun all relevant commands independently, and record whether the slice is implemented, tested, independently verified, Owner accepted, committed, pushed, and published.
- Unknown dirty/untracked work is never deleted, overwritten, or silently absorbed into a claim.

## Current Stop Condition

There is no active slice and no active worker. V10-E01 remains blocked until the Owner uses the explicit local capture command against Diablo IV to produce the exact client-surface cases and supplies hash-bound BD/role labels, provenance, HUD-hidden confirmation, stable UI-scale confirmation, and privacy clearance. The receipt narrows coordinate and file-integrity ambiguity but does not supply those facts. Do not tune the detector from unknown-space smoke material or create a target manifest, candidate freeze, package, signature, commit, push, upload, or publication while the inputs are absent.

## 2026-07-23 Initial Manager Review (Pre-Intake)

This historical section predates the current 17-file Owner intake. Its `0/12`, missing-manifest exit `2`, smoke-only, and `37/37` facts remain valid for that earlier corpus state; the current corpus review is recorded below.

## 2026-07-23 V10-E03-S1 Manager Review

- Worker delivery stayed within five paths: `tests/D4Hub.VisionProbe/Program.cs`, new `tests/vision-replay/verify-client-capture-receipt.ps1`, `scripts/verify.ps1`, `tests/vision-replay/README.md`, and `docs/manager/evidence/v1.0/V10-E03-S1-provisional-2026-07-23.md`. The pre/post combined SHA-256 digest for 72 non-fence repository files matched exactly.
- Receipt behavior: both existing commands retain their capture paths and overwrite behavior. Success emits exactly nine privacy-minimized fields after the saved PNG is decoded and measured twice: schema/mode, `client-device-pixels`, method, dimensions, byte length, SHA-256, and absolute output path. No title, handle, position, pixels, or visible content is emitted.
- Independent focused test: exit `0`; PrintWindow client-only and client-rect screen-copy both captured the visible local fixture, produced matching client dimensions, replaced explicit placeholder outputs, and bound PNG header/bytes/hash/path correctly. Both missing-window cases returned exit `3`; invalid-parent write returned exit `5`; all emitted no success receipt and left no output.
- Independent canonical verification: exit `0`; Release build `0` warnings/`0` errors; acceptance `44/44`; E02 coordinate contract PASS; E03 receipt contract PASS; forbidden capability matches `0`; `git diff --check` and direct whitespace scan passed. Fixture processes and `d4hub-v10-e03-*` temporary directories remaining after verification: `0`.
- Residual boundary: fixture success proves only this interactive Windows host. PrintWindow may differ by compositor/application, and screen copy requires visible unobscured content. The receipt proves local coordinate/file binding only, not Diablo or Owner provenance, screenshot privacy, labels, replay success, BD behavior, candidate acceptance, or release readiness. Manager Memory `memory_3864963c0cf7ef22f4b7630ee8c70f61` is recorded at project sequence `60`.

## 2026-07-23 V10-E02-S2 Manager Review

- Worker writes were limited to the ignored exploratory smoke manifest/report and `docs/manager/evidence/v1.0/V10-E02-S2-provisional-2026-07-23.md`. The pre/post combined digest for 74 non-fence repository files matched exactly, and the combined name/byte/SHA-256 digest for all 17 original PNGs matched exactly.
- Manifest review found schema `1`, evidence class `smoke`, 17 unique opaque IDs, 17 unique existing paths, no missing or extra PNG, `captureSpace: unknown`, threshold `0.55`, and expected decision `accepted` for every case.
- Independent replay returned exit `5`, report passed `false`, actual accepted `6`, rejected `11`, and the same 11 failed IDs as the worker run. Repeated manager runs preserved all decisions, dimensions, confidences, bounds, fingerprints, and failure messages; only top-level `GeneratedAt` and accepted-fingerprint `capturedAt` timestamps changed.
- Independent canonical verification returned exit `0`: Release build `0` warnings/`0` errors, acceptance `44/44`, coordinate contract PASS, and repository PASS. `git diff --check` and provisional-report whitespace checks passed.
- Acceptance boundary: the failed smoke result itself is accepted as accurate, independently reproduced diagnostic evidence. It does not establish a runtime client-surface defect because capture space and provenance are unknown, and it does not satisfy V10-E01, BD stability/distinction, Owner acceptance, or candidate evidence. Manager Memory `memory_0136ea930a50933cbddf780f9823b119` is recorded at project sequence `58`.

## 2026-07-23 V10-E02-S1 Manager Review

- Worker report: `docs/manager/evidence/v1.0/V10-E02-S1-provisional-2026-07-23.md`; the complete six-path delivery stayed within the allowed replay contract/test/documentation fence.
- Independent reruns: `tests/vision-replay/verify-coordinate-contract.ps1` exit `0`; canonical `scripts/verify.ps1` exit `0` with Release build `0` warnings/`0` errors, acceptance `44/44`, contract PASS, and repository PASS; `git diff --check` exit `0`.
- Contract result: target cases must declare `captureSpace: client-device-pixels`; missing, `unknown`, and `full-window` target declarations fail with exit `2` before image/build/probe and create no report. Schema-only success is explicitly `not-replay-evidence` and reports only `RequiredResolutionRepresentativesPresent`, never the removed misleading `TargetResolutionGateSatisfied` field.
- Fence result: all non-fence repository SHA-256 baselines and all 17 original PNG hashes matched the pre-dispatch inventory. No target manifest or replay evidence was created.
- Slice classification: `requirements defined=yes`, `implemented=yes`, `tested=yes`, `independently verified=yes within the replay-contract boundary`; `owner accepted=no`, `packaged=no`, `candidate SHA=none`, `committed=no`, `pushed=no`, `published=no`. Manager Memory `memory_d7d5b9c3d14b7bb916e8b2b942d838f8` is recorded at project sequence `56`.

- Worker report: `docs/manager/evidence/v1.0/V10-E01-provisional-2026-07-23.md`; provisional only. The worker changed no product, test, script, library, PKR, package, artifact, workflow, or reference file.
- Independent corpus check: `.artifacts/v0.2-real-frames/target/` is absent; raw target inputs are `0/12`. The only PNG in the permitted subtree is `中文截图.png`, `1404x841`, `1,975,010` bytes, SHA-256 `D04C6F5035ADED378C36707581CE5CC62D420364AB46353FFF1D27955CE3994F` (smoke/non-gating).
- Independent target-manifest replay: `powershell -ExecutionPolicy Bypass -File .\scripts\verify-vision-replay.ps1 -Manifest .\.artifacts\v0.2-real-frames\target\manifest.json -Output .\.artifacts\v0.2-real-frames\target\manager-missing-report.json -NoBuild` returned exit `2`, `Manifest not found`; no report was created.
- Independent smoke replay: existing `smoke-manifest.json` returned exit `0`, one `1404x841` accepted case, `EvidenceClass: smoke`; this remains non-gating.
- Independent canonical baseline: `powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1` returned exit `0`, Release build 0 warnings/0 errors, all `37/37` deterministic acceptance checks, safety scan, whitespace check, and `PASS repository verification`.
- Manager acceptance: the worker's **factual blocker delivery is accepted** because every material claim was independently reproduced. V10-E01 itself remains **blocked**, not independently verified as a target gate. v0.2/V02-S3 remains blocked; no candidate SHA, package hash, Owner acceptance, commit, push, or publication exists.

## 2026-07-23 V10-D01-S1 Manager Review

- Worker report: `docs/manager/evidence/v1.0/V10-D01-S1-provisional-2026-07-23.md`; provisional delivery inspected line-by-line within the five-file fence.
- Independent reruns: Release build exit `0` with 0 warnings/0 errors; AcceptanceTests exit `0` with `44/44`; canonical `scripts/verify.ps1` exit `0` with `PASS repository verification`; `git diff --check` exit `0`; forbidden-capability scan over the four fenced code/test files returned no matches.
- Independent behavior review: startup `Load()` fallback remains intact; strict import rejects missing/malformed/empty/null/schema-invalid/selected-profile-invalid documents; both UI import paths validate and persist before replacement and convert failure to status text; focused tests prove active reference, content, persisted bytes, SHA-256, and temporary-file preservation after failed import.
- Slice classification: `implemented=yes`, `tested=yes`, `independently verified=yes` for this local deterministic boundary; `owner accepted=no`, `packaged=no`, `candidate SHA=none`, `committed=no`, `pushed=no`, `published=no`.
- Residual boundary: WPF physical/device behavior, power-loss durability, clean-machine/offline operation, full recovery matrix, target-resolution evidence, Owner acceptance, and all formal release actions remain open or deferred.

## 2026-07-23 V10-E01 Corpus Intake Manager Review

- Worker delivery: one bounded retry of the existing `v10_e01_evidence` worker completed. The only worker write is `docs/manager/evidence/v1.0/V10-E01-corpus-intake-provisional-2026-07-23.md`; no second worker was dispatched. The worker report is provisional and does not claim target acceptance.
- Independent inventory: the manager read the report and independently re-read every original PNG. A structured PNG-header/byte/SHA-256 check matched every report row (`17/17`); the target directory still has no `manifest.json`, exact `1920x1080` is `0`, and exact `1280x960` is `0`.
- Independent visual result: all 17 visibly contain a full client/game surface, an open character panel, and a visible skill bar. D4Hub HUD is `uncertain` from pixels alone. Similar layouts are only repeat-pair candidates. BD-A/BD-B identity, repeated-pair binding, unknown-BD and panel-absent roles, provenance, and privacy clearance are unproven. Visible player/character labels and stats/currency remain a privacy risk; no such text was copied into tracked evidence.
- Independent commands: `powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1` passed Release build `0` warnings/`0` errors and `44/44` deterministic checks; `git diff --check` passed. No target replay was run because the required manifest and exact target inputs are absent.
- Acceptance boundary: the manager accepts the worker delivery only as accurate **exploratory/smoke blocker evidence**. It does not implement, test, independently verify, or Owner-accept the V10-E01 target gate. PKR Task `task_dd62a03b6e0028db49e2dda9417d3581` remains `backlog`; no Assignment, Verification, or Acceptance was created. Manager Memory `memory_b1e05cd30e79d2fbaf9ae6bef5df2dcc` is derived from Task revision `1` at project sequence `54`.
- Remaining Owner inputs: twelve raw, exact-resolution captures (known BD-A twice and known BD-B twice per resolution; one unknown/unregistered-BD panel-open and one panel-absent per resolution) at `1920x1080` and `1280x960`; exact-hash-bound labels and provenance; HUD-hidden confirmation; stable UI scale within pairs; visible skill bar; no crop/resize/composite/re-encode; and privacy-cleared account/chat surface.
- State classification for this slice: `implemented=report only`; `tested=metadata/visual intake plus canonical local checks`; `independently verified=manager-verified intake facts only`; `owner accepted=no`; `packaged=no`; `candidate SHA=none`; `committed=no`; `pushed=no`; `published=no`.
