<Window x:Class="D4Hub.App.MapOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="D4Hub 开荒地图"
        Width="440"
        Height="520"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        IsHitTestVisible="False"
        Background="Transparent"
        Topmost="True"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        SourceInitialized="Window_SourceInitialized"
        Closing="Window_Closing"
        SizeChanged="Window_SizeChanged">
    <Window.Resources>
        <SolidColorBrush x:Key="MapHudSurfaceBrush" Color="#E61A1713" />
        <SolidColorBrush x:Key="MapHudBorderBrush" Color="#B57A6855" />
        <SolidColorBrush x:Key="MapHudAccentBrush" Color="#FFC4A060" />
        <SolidColorBrush x:Key="MapHudValueBrush" Color="#FFF7E7C3" />
        <Style x:Key="MapHudTimerLabel" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Foreground" Value="#FFB9B5AD" />
        </Style>
        <Style x:Key="MapHudTimerValue" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource MapHudValueBrush}" />
        </Style>
        <Style x:Key="MapHudTimerStatus" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
            <Setter Property="FontSize" Value="9" />
            <Setter Property="Foreground" Value="#FF8A8578" />
        </Style>
    </Window.Resources>
    <Grid>
        <Border Padding="6"
                Background="{StaticResource MapHudSurfaceBrush}"
                BorderBrush="{StaticResource MapHudBorderBrush}"
                BorderThickness="1"
                CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- 顶部计时条：地狱狂潮 / 世界 Boss / 军团 Boss -->
                <StackPanel x:Name="TimerBar" Grid.Row="0" Margin="2,2,2,6">
                    <Border x:Name="HelltideBar" Background="#33000000" CornerRadius="3" Margin="0,0,0,3" Padding="6,3">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="地狱狂潮" Style="{StaticResource MapHudTimerLabel}" Foreground="#FFFF6B35" VerticalAlignment="Center" />
                            <TextBlock x:Name="HelltideRemaining" Margin="8,0,0,0" Text="--:--" Style="{StaticResource MapHudTimerValue}" />
                            <TextBlock x:Name="HelltideStatus" Margin="8,0,0,0" Text="推算中" Style="{StaticResource MapHudTimerStatus}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <Border x:Name="WorldBossBar" Background="#33000000" CornerRadius="3" Margin="0,0,0,3" Padding="6,3">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="世界Boss" Style="{StaticResource MapHudTimerLabel}" Foreground="#FF9B59B6" VerticalAlignment="Center" />
                            <TextBlock x:Name="WorldBossRemaining" Margin="8,0,0,0" Text="--:--" Style="{StaticResource MapHudTimerValue}" />
                            <TextBlock x:Name="WorldBossStatus" Margin="8,0,0,0" Text="推算中" Style="{StaticResource MapHudTimerStatus}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <Border x:Name="LegionBar" Background="#33000000" CornerRadius="3" Padding="6,3">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="军团Boss" Style="{StaticResource MapHudTimerLabel}" Foreground="#FFE74C3C" VerticalAlignment="Center" />
                            <TextBlock x:Name="LegionRemaining" Margin="8,0,0,0" Text="--:--" Style="{StaticResource MapHudTimerValue}" />
                            <TextBlock x:Name="LegionStatus" Margin="8,0,0,0" Text="推算中" Style="{StaticResource MapHudTimerStatus}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- 地图底图 + POI 标记叠加 -->
                <Grid x:Name="MapHost" Grid.Row="1" ClipToBounds="True">
                    <Image x:Name="MapImage"
                           Stretch="Fill"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           SnapsToDevicePixels="False" />
                    <Canvas x:Name="PoiLayer" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
